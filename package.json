{
  "name": "electronic-ecommerce",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,blue \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w api && npm run build -w web",
    "start": "npm run start -w api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
